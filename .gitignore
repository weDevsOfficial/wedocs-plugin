config.codekit
.codekit-cache
plugin-deploy.sh

# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
/node_modules
/vendor/

.vscode/
.idea/
wedocs*.zip

# Release / dist artifacts
wedocs*.zip
*.zip
/build/
/dist/

# Built assets — gitignored, NOT committed. CI runs `npm run build` and the 10up
# deploy ships the on-disk working tree to wp.org (like wedevs-project-manager
# ships its gitignored /views/assets/dist). Do NOT use BUILD_DIR — it strips this.
assets/build/

config.codekit
.codekit-cache
plugin-deploy.sh

# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
/node_modules
/vendor/

.vscode/
.idea/
wedocs*.zip

# Release / dist artifacts
wedocs*.zip
*.zip
/build/
/dist/

# NOTE: assets/build/ IS tracked — the wp.org SVN deploy ships only git-tracked
# files, so the built blocks/JS must be committed or activation fatals.

# Built assets — gitignored; CI builds + ships them via BUILD_DIR (not committed)
assets/build/

/**
 * Save function for the Social Share block
 *
 * Since we're using dynamic rendering with render.php,
 * we return null here to let WordPress handle the rendering server-side
 */
const Save = () => {
    return null;
};

export default Save;
